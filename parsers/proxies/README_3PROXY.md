# 🌐 Настройка 3proxy для Ozon Parser

## 📋 Содержание
1. [Быстрый старт](#быстрый-старт)
2. [Структура](#структура)
3. [Как это работает](#как-это-работает)
4. [Ротация прокси](#ротация-прокси)
5. [Проверка](#проверка)

---

## ⚡ Быстрый старт

### 1️⃣ Генерируем конфиг из upstreams.txt

```bash
cd proxies/
python generate_3proxy_config.py
```

Результат:
```
✅ Загружено 40 прокси
✅ Конфиг сгенерирован: 3proxy.cfg
   • Upstream прокси: 40
   • Локальные порты: 31280-31399 (120 портов)
```

### 2️⃣ Запускаем 3proxy

**Windows:**
```bash
run_3proxy.bat
```

**Linux/Mac:**
```bash
./3proxy 3proxy.cfg
```

### 3️⃣ Проверяем что работает

```bash
# Тест порта 31280
curl -x http://127.0.0.1:31280 http://ipinfo.io/ip

# Должен вернуть IP одного из прокси
```

---

## 📦 Структура

```
proxies/
├── upstreams.txt              ← Список 40 прокси
├── 3proxy.cfg                 ← Авто-сгенерированный конфиг
├── generate_3proxy_config.py  ← Генератор конфига
├── run_3proxy.bat             ← Батник для запуска (Windows)
└── 3proxy.exe                 ← Исполняемый файл (нужно скачать)
```

---

## ⚙️ Как это работает

### Структура конфига

```
upstreams.txt (40 прокси)
    ↓
    ├─ parent 1000 → 81.177.181.208:17357
    ├─ parent 1001 → 91.243.60.163:10448
    ├─ parent 1002 → 146.19.13.219:21490
    └─ ... 37 еще прокси
    
Локальные порты (3proxy)
    ├─ 31280 → parent 1000  (прокси #1)
    ├─ 31281 → parent 1001  (прокси #2)
    ├─ 31282 → parent 1002  (прокси #3)
    └─ ... 117 еще портов
```

### Как браузер использует прокси

```
Браузер 1 на порту 9222 → использует прокси 31280 → IP из прокси #1
Браузер 2 на порту 9223 → использует прокси 31281 → IP из прокси #2
Браузер 3 на порту 9224 → использует прокси 31282 → IP из прокси #3
... и так далее
```

---

## 🔄 Ротация прокси

### Когда срабатывает ротация?

Когда парсер обнаруживает блок/капчу:

```python
# Ozon3.py
if "antibot" in page_html or "captcha" in page_html:
    rotate_proxy_for_attached_port(dm)  # ← Ротирует прокси
    return None, 'recycle'               # ← Убивает браузер и открывает новый
```

### Как ротация работает?

3proxy имеет встроенные эндпоинты для ротации:

```bash
# Ротирует прокси на порту 31280
curl http://127.0.0.1:31280/rotate

# Альтернативные эндпоинты
curl http://127.0.0.1:31280/api/rotate
curl http://127.0.0.1:31280/refresh
curl http://127.0.0.1:31280/change-ip
```

> ℹ️ Парсер пытается все эти URL по очереди (см. `ROTATE_URL_FALLBACKS` в `Ozon3.py`)

---

## ✅ Проверка

### Проверка конфига

```bash
# Посмотреть список прокси
grep "^parent" proxies/3proxy.cfg | head -5

# Посмотреть список портов
grep "proxy -n -a" proxies/3proxy.cfg | head -5
```

### Тест прокси

```bash
# Windows PowerShell
curl -proxy http://127.0.0.1:31280 -Uri "http://ipinfo.io/json"

# Linux/Mac
curl -x http://127.0.0.1:31280 http://ipinfo.io/json

# Результат - IP одного из прокси:
# {
#   "ip": "81.177.181.208",
#   "city": "Moscow",
#   ...
# }
```

### Проверка ротации

```bash
# Запрос IP с ротацией
for i in {1..3}; do
    curl -s -x http://127.0.0.1:31280 http://ipinfo.io/ip
    curl -s http://127.0.0.1:31280/rotate > /dev/null
    sleep 1
done

# Должны вернуться разные IP
```

---

## 🐛 Решение проблем

### ❌ "3proxy.exe не найден"

Скачайте с https://3proxy.org/

Распакуйте в `proxies/`

### ❌ "Connection refused на 31280"

Проверьте что 3proxy запущен:
```bash
netstat -ano | findstr "31280"
```

Если пусто → запустите `run_3proxy.bat`

### ❌ "Failed to connect to upstream proxy"

Проверьте upstreams.txt:
```bash
# Первая строка должна быть вроде:
# 81.177.181.208:17357:BF5SGEaPX4:FKvnm5pAqg
```

Если формат неправильный - отредактируйте вручную.

### ❌ "Ротация не работает"

Проверьте что ROTATE_ON_CAPTCHA=1 в `.env`:
```bash
ROTATE_ON_CAPTCHA=1
PROXY_START=31280
```

---

## 📊 Статистика

| Параметр | Значение |
|----------|----------|
| Upstream прокси | 40 |
| Локальные порты | 31280-31399 (120 шт) |
| Пропускная способность | ~100 одновременных браузеров |
| Ротация | Автоматическая при блоке |

---

## 📝 Примечания

- ✅ Каждый браузер получает свой прокси
- ✅ При блоке парсер автоматически ротирует IP
- ✅ При рециклу браузера - применяется новый User-Agent
- ✅ 3proxy - бесплатный, легковесный, надежный
- ✅ Конфиг автоматически генерируется из upstreams.txt

**Автор:** Парсер Ozon3.py
