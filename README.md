# Ozon/Wildberries Price Parser

Автоматизированная система парсинга цен с маркетплейсов Ozon и Wildberries с веб-интерфейсом управления.

## 🚀 Быстрый старт

### Требования
- **Python 3.8+**
- **PostgreSQL 12+**
- **Git**

### Установка одной командой

```bash
# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd ParserProd

# 2. Запустите автоматическую установку
python setup.py
```

Скрипт `setup.py` автоматически:
- ✅ Проверит версию Python
- ✅ Проверит наличие PostgreSQL
- ✅ Установит все Python зависимости
- ✅ Создаст файл `.env` с вашими настройками
- ✅ Создаст базу данных PostgreSQL
- ✅ Создаст все необходимые таблицы
- ✅ Создаст администратора по умолчанию
- ✅ Создаст файл `config.json`

### После установки

1. **Запустите веб-интерфейс:**
   ```bash
   python web_app.py
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:3455
   ```

3. **Войдите:**
   - Логин: `admin`
   - Пароль: `admin`
   - ⚠️ **Смените пароль после первого входа!**

4. **Настройте в веб-интерфейсе:**
   - URL Google Sheets для Ozon и Wildberries
   - Telegram Bot Token и Chat ID (опционально)
   - Расписание автоматического парсинга

5. **Импортируйте данные:**
   - Перейдите в раздел "Настройки"
   - Нажмите "Импортировать OZON"
   - Нажмите "Импортировать WB"

## 📋 Возможности

### Основные функции
- 🔍 **Парсинг цен** с Ozon и Wildberries
- 📊 **Генерация Excel отчётов** с автоматической отправкой в Telegram
- 🌐 **Веб-интерфейс** для управления и мониторинга
- ⏰ **Планировщик задач** для автоматического парсинга
- 👥 **Управление пользователями** с разными уровнями доступа
- 📈 **История цен** и аналитика
- 🔄 **Импорт данных** из Google Sheets

### Поддерживаемые маркетплейсы
- ✅ **Ozon** - полная поддержка
- ✅ **Wildberries** - полная поддержка

## 📁 Структура проекта

```
ParserProd/
├── setup.py                          # Автоматическая установка
├── web_app.py                        # Веб-интерфейс (Flask)
├── ozon_parser_production_final.py   # Парсер Ozon
├── wb_parser_production.py           # Парсер Wildberries
├── generate_report.py                # Генерация отчёта Ozon
├── wb_reporting.py                   # Генерация отчёта WB
├── import_from_sheets.py             # Импорт данных Ozon
├── import_from_wb_sheets.py          # Импорт данных WB
├── setup_database_and_users.py       # Настройка БД
├── scheduler.py                      # Планировщик задач
├── requirements.txt                  # Python зависимости
├── .env                              # Конфигурация (создаётся при установке)
├── config.json                       # Настройки (создаётся при установке)
├── templates/                        # HTML шаблоны
│   ├── dashboard.html
│   ├── settings.html
│   └── ...
└── proxies/                          # Прокси-сервер
```

## 🔧 Ручная установка

Если автоматическая установка не работает:

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Создайте файл `.env`

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ozon_parser
DB_USER=postgres
DB_PASS=your_password

# Flask
FLASK_SECRET_KEY=your_secret_key_here

# Telegram (опционально)
TG_BOT_TOKEN=
TG_CHAT_ID=
```

### 3. Создайте базу данных

```bash
# PostgreSQL
psql -U postgres -c "CREATE DATABASE ozon_parser;"

# Создайте таблицы
python setup_database_and_users.py
```

### 4. Создайте `config.json`

```json
{
  "ozon_spreadsheet_url": "",
  "wb_spreadsheet_url": ""
}
```

## 📖 Использование

### Веб-интерфейс

```bash
python web_app.py
```

Откройте http://localhost:3455

**Разделы:**
- **Dashboard** - статистика и мониторинг
- **Настройки** - конфигурация парсеров
- **Пользователи** - управление доступом

### Парсинг вручную

**Ozon:**
```bash
python ozon_parser_production_final.py
```

**Wildberries:**
```bash
python wb_parser_production.py
```

### Генерация отчётов

**Ozon:**
```bash
python generate_report.py
```

**Wildberries:**
```bash
python wb_reporting.py
```

### Импорт данных

**Ozon:**
```bash
python import_from_sheets.py
```

**Wildberries:**
```bash
python import_from_wb_sheets.py
```

## 🔐 Настройка Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите Bot Token
3. Добавьте бота в ваш чат/группу
4. Получите Chat ID через [@userinfobot](https://t.me/userinfobot)
5. Настройте в веб-интерфейсе (Настройки → Telegram)

## 📊 Формат отчётов

Отчёты генерируются в формате Excel с колонками:

| SKU | Продавец | Промо | Цена | Старая |
|-----|----------|-------|------|--------|
| 142892882 | ООО Дилер | 20 391 ₽ | 25 069 ₽ | |

**Особенности:**
- Автофильтры на всех колонках
- Заморозка заголовка
- Цветовая кодировка (синий - Ozon, фиолетовый - WB)
- Автоматическая отправка в Telegram

## 🛠️ Полезные скрипты

```bash
# Проверка подключения к БД
python test_db_connection.py

# Проверка Telegram
python test_telegram.py

# Очистка всех данных
python clear_all_data.py

# Статистика БД
python check_db_stats_full.py
```

## 🔄 Обновление проекта

```bash
git pull origin main
pip install -r requirements.txt --upgrade
```

## 📝 Логи

Логи сохраняются в:
- `web_log.txt` - логи веб-интерфейса
- `web_err.txt` - ошибки веб-интерфейса
- Консоль - логи парсеров

## ⚙️ Установка как Windows Service

См. [SERVICE_README.md](SERVICE_README.md)

## 🌐 Веб-интерфейс

Подробная документация: [WEB_README.md](WEB_README.md)

## 🐛 Устранение неполадок

### PostgreSQL не найден

**Windows:**
```bash
# Добавьте в PATH:
C:\Program Files\PostgreSQL\15\bin
```

**Linux:**
```bash
sudo apt-get install postgresql postgresql-contrib
```

### Ошибка подключения к БД

Проверьте:
1. PostgreSQL запущен
2. Правильные учётные данные в `.env`
3. База данных создана

```bash
# Проверка статуса PostgreSQL
# Windows:
pg_ctl status

# Linux:
sudo systemctl status postgresql
```

### Ошибка импорта зависимостей

```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Telegram не отправляет отчёты

1. Проверьте Bot Token и Chat ID
2. Убедитесь, что бот добавлен в чат
3. Запустите тест: `python test_telegram.py`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи (`web_log.txt`, `web_err.txt`)
2. Запустите диагностические скрипты
3. Проверьте документацию в папке проекта

## 📄 Лицензия

Проект для внутреннего использования.

## 🔖 Версия

Текущая версия: **2.0**
- ✅ Поддержка Wildberries
- ✅ Новый формат отчётов
- ✅ Настройки Telegram в UI
- ✅ Автоматическая установка
