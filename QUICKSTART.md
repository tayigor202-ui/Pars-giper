# 🚀 БЫСТРЫЙ СТАРТ

## Установка за 3 шага

### Windows

```cmd
# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd ParserProd

# 2. Запустите установку (двойной клик или в терминале)
install.bat

# 3. Запустите веб-интерфейс
start_web.bat
```

### Linux / macOS

```bash
# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd ParserProd

# 2. Сделайте скрипты исполняемыми и запустите установку
chmod +x install.sh start_web.sh
./install.sh

# 3. Запустите веб-интерфейс
./start_web.sh
```

### Альтернативный способ (все платформы)

```bash
# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd ParserProd

# 2. Запустите установку
python setup.py

# 3. Запустите веб-интерфейс
python web_app.py
```

## Что происходит при установке?

Скрипт `setup.py` автоматически:

1. ✅ **Проверяет Python** (требуется 3.8+)
2. ✅ **Проверяет PostgreSQL** (требуется 12+)
3. ✅ **Устанавливает зависимости** из `requirements.txt`
4. ✅ **Создаёт .env файл** (спросит параметры БД)
5. ✅ **Создаёт базу данных** PostgreSQL
6. ✅ **Создаёт таблицы** и структуру БД
7. ✅ **Создаёт администратора** (admin/admin)
8. ✅ **Создаёт config.json** для настроек

## После установки

1. **Откройте браузер:**
   ```
   http://localhost:3455
   ```

2. **Войдите:**
   - Логин: `admin`
   - Пароль: `admin`
   - ⚠️ Смените пароль!

3. **Настройте:**
   - URL Google Sheets (Ozon и WB)
   - Telegram Bot Token и Chat ID
   - Расписание парсинга

4. **Импортируйте данные:**
   - Нажмите "Импортировать OZON"
   - Нажмите "Импортировать WB"

5. **Готово!** 🎉

## Требования

### Обязательно
- **Python 3.8+** - [Скачать](https://www.python.org/downloads/)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)
- **Git** - [Скачать](https://git-scm.com/downloads)

### Опционально
- **Google Sheets API** - для импорта данных
- **Telegram Bot** - для отправки отчётов

## Проблемы?

### Python не найден

**Windows:**
- Переустановите Python
- Отметьте "Add Python to PATH"

**Linux/macOS:**
```bash
# Ubuntu/Debian
sudo apt-get install python3 python3-pip

# macOS
brew install python3
```

### PostgreSQL не найден

**Windows:**
- Добавьте в PATH: `C:\Program Files\PostgreSQL\15\bin`

**Linux:**
```bash
sudo apt-get install postgresql postgresql-contrib
```

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

### Ошибка при установке зависимостей

```bash
# Обновите pip
python -m pip install --upgrade pip

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

## Полная документация

См. [README.md](README.md) для подробной информации.

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `web_log.txt`, `web_err.txt`
2. Запустите: `python test_db_connection.py`
3. Проверьте настройки в `.env`

---

**Версия:** 2.0  
**Последнее обновление:** 27.01.2026
